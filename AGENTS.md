# AGENTS.md

用于减少 LLM 常见编码错误的行为准则。可按需与项目专属指令合并使用。

**取舍：** 这些准则更偏向谨慎，而不是速度。对于非常简单的任务，可自行判断。

## 0. 规则前置

当任务涉及生成代码、技术方案、重构、Bug 修复、依赖选择或架构取舍时，必须先读取并遵守：

```text
vibe-coding/documents/common/最小实现原则.md
```

如果项目中不存在该文件，则使用本文件的“简单优先”和“外科手术式修改”原则作为降级规则。

## 1. 编码前先思考

**不要假设。不要隐藏困惑。要明确说出取舍。**

实现前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解，列出来，不要默默选择其中一种。
- 如果有更简单的做法，要说出来。必要时提出反对意见。
- 如果有内容不清楚，先停下来。说明哪里不清楚，然后提问。

## 2. 简单优先

**用能解决问题的最少代码。不要写推测性内容。**

- 不实现需求之外的功能。
- 不为单次使用的代码创建抽象。
- 不添加未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景编写错误处理。
- 如果你写了 200 行，而 50 行就能解决，重写。

问问自己：“资深工程师会不会认为这太复杂？”如果答案是会，就简化。

## 3. 外科手术式修改

**只改必须改的地方。只清理你自己造成的问题。**

编辑现有代码时：

- 不要顺手“改进”相邻代码、注释或格式。
- 不要重构没有问题的代码。
- 遵循现有风格，即使你个人会采用不同写法。
- 如果发现无关的死代码，提出来，但不要删除。

当你的修改产生了孤立代码时：

- 删除因为你的修改而变成未使用的 import、变量、函数。
- 不要删除原本就存在的死代码，除非用户明确要求。

检验标准：每一行改动都应该能直接追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环执行直到验证通过。**

把任务转化为可验证的目标：

- “添加校验” → “为非法输入编写测试，然后让测试通过”
- “修复 bug” → “编写能复现问题的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，先给出简短计划：

```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

明确的成功标准可以让你独立循环推进。模糊标准（例如“让它能用”）会导致反复澄清。

---

**如果这些准则有效，应该表现为：** diff 中不必要的改动更少，因为过度复杂导致的返工更少，并且澄清问题发生在实现前，而不是错误发生后。
